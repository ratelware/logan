CMAKE_MINIMUM_REQUIRED(VERSION 2.8.1)
CMAKE_POLICY(VERSION 2.8.1)

PROJECT(LoganUI)

INCLUDE_DIRECTORIES(${PRODUCTION_INCLUDE_DIR})

ADD_SUBDIRECTORY(executor)
ADD_SUBDIRECTORY(ui)

SET(INTERNAL_LIBRARIES executor ui PARENT_SCOPE)